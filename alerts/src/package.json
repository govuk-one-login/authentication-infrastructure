{
  "name": "alerts-src",
  "version": "1.0.0",
  "main": "alerts.js",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-sts": "3.994.0",
    "@aws-sdk/client-sns": "3.994.0"
  },
  "scripts": {
    "build": "mkdir -p ../dist && zip -r ../dist/alerts.zip ."
  }
}
